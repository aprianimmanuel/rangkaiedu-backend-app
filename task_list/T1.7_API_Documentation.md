# Rangkai Edu - T1.7 Class & Content Management API Documentation

## Table of Contents
1. [Overview](#overview)
2. [Authentication and Authorization](#authentication-and-authorization)
3. [Classes API](#classes-api)
   - [Create Class](#create-class)
   - [Get All Classes](#get-all-classes)
   - [Get Class by ID](#get-class-by-id)
   - [Update Class](#update-class)
   - [Delete Class](#delete-class)
4. [Subjects API](#subjects-api)
   - [Create Subject](#create-subject)
   - [Get All Subjects](#get-all-subjects)
   - [Get Subject by ID](#get-subject-by-id)
   - [Update Subject](#update-subject)
   - [Delete Subject](#delete-subject)
5. [Class Roster Management API](#class-roster-management-api)
   - [Add Student to Class](#add-student-to-class)
   - [Get Class Roster](#get-class-roster)
   - [Remove Student from Class](#remove-student-from-class)
   - [Update Student Enrollment Status](#update-student-enrollment-status)
6. [Materials API](#materials-api)
   - [Upload Teaching Material](#upload-teaching-material)
   - [Get All Materials](#get-all-materials)
   - [Get Material by ID](#get-material-by-id)
   - [Update Material](#update-material)
   - [Delete Material](#delete-material)
   - [Download Material](#download-material)
7. [Data Models](#data-models)
   - [Class Model](#class-model)
   - [Subject Model](#subject-model)
   - [Material Model](#material-model)
   - [Student Enrollment Model](#student-enrollment-model)
8. [Error Responses](#error-responses)
9. [Integration Information](#integration-information)

## Overview

This document provides comprehensive documentation for the T1.7 Class & Content Management API for the Rangkai Edu project. This API enables teachers to manage classes, subjects, class rosters, and educational materials. The API follows REST conventions and uses JSON for request/response formats.

## Authentication and Authorization

All endpoints require a valid JWT token in the Authorization header:
```
Authorization: Bearer <token>
```

The API uses role-based access control (RBAC) with the following roles:
- `admin` - System administrators with full access
- `teacher` - Educators with access to teaching resources
- `student` - Learners with access to course materials
- `parent` - Guardians with access to student progress information

### Role-Based Access Control

| Endpoint | Required Role(s) | Notes |
|----------|------------------|-------|
| POST /api/classes | Teacher, Admin | Teachers can only create classes in their school |
| GET /api/classes | Teacher, Admin | Teachers can only see classes in their school |
| GET /api/classes/{id} | Teacher, Admin | User must belong to the same school as the class |
| PUT /api/classes/{id} | Teacher, Admin | Teachers can only update classes in their school |
| DELETE /api/classes/{id} | Admin | Admin only |
| POST /api/subjects | Teacher, Admin | Teachers can only create subjects in their school |
| GET /api/subjects | Teacher, Admin | Teachers can only see subjects in their school |
| GET /api/subjects/{id} | Teacher, Admin | User must belong to the same school as the subject |
| PUT /api/subjects/{id} | Teacher, Admin | Teachers can only update subjects in their school |
| DELETE /api/subjects/{id} | Admin | Admin only |
| POST /api/classes/{class_id}/students | Teacher, Admin | Teacher must be assigned to the class |
| GET /api/classes/{class_id}/students | Teacher, Admin | Teacher must be assigned to the class |
| DELETE /api/classes/{class_id}/students/{student_id} | Teacher, Admin | Teacher must be assigned to the class |
| PUT /api/classes/{class_id}/students/{student_id} | Teacher, Admin | Teacher must be assigned to the class |
| POST /api/materials | Teacher, Admin | Teachers can only upload materials for classes they teach |
| GET /api/materials | Any authenticated user | Access based on visibility settings |
| GET /api/materials/{id} | Any authenticated user | Access based on visibility settings |
| PUT /api/materials/{id} | Teacher, Admin | Teacher must be the owner of the material |
| DELETE /api/materials/{id} | Teacher, Admin | Teacher must be the owner of the material |
| GET /api/materials/{id}/download | Any authenticated user | Access based on visibility settings |

## Classes API

### Create Class

Creates a new class.

**Endpoint:** `POST /api/classes`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teachers can only create classes in their school
- Admins can create classes in any school

**Request Body:**
```json
{
  "school_id": "uuid",
  "name": "Class 1A",
  "grade_level": 1,
  "academic_year": "2024-2025"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| school_id | string (UUID) | Yes | ID of the school | Must be a valid UUID |
| name | string | Yes | Name of the class | Max 100 characters |
| grade_level | integer | Yes | Grade level of the class | Between 1 and 12 |
| academic_year | string | Yes | Academic year | Non-empty string |

**Success Response (201 Created):**
```json
{
  "id": "uuid",
  "school_id": "uuid",
  "name": "Class 1A",
  "grade_level": 1,
  "academic_year": "2024-2025",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `500 Internal Server Error` - Server errors

### Get All Classes

Retrieves all classes with optional filtering.

**Endpoint:** `GET /api/classes`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teachers can only see classes in their school
- Admins can see all classes

**Query Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| school_id | string (UUID) | No | Filter by school |
| grade_level | integer | No | Filter by grade level |
| academic_year | string | No | Filter by academic year |

**Success Response (200 OK):**
```json
[
  {
    "id": "uuid",
    "school_id": "uuid",
    "name": "Class 1A",
    "grade_level": 1,
    "academic_year": "2024-2025",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
]
```

**Error Responses:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `500 Internal Server Error` - Server errors

### Get Class by ID

Retrieves a specific class by ID.

**Endpoint:** `GET /api/classes/{id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- User must belong to the same school as the class or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the class |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "school_id": "uuid",
  "name": "Class 1A",
  "grade_level": 1,
  "academic_year": "2024-2025",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid class ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Class not found
- `500 Internal Server Error` - Server errors

### Update Class

Updates an existing class.

**Endpoint:** `PUT /api/classes/{id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must belong to the same school as the class
- Admins can update any class

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the class |

**Request Body:**
```json
{
  "school_id": "uuid",
  "name": "Class 1A Updated",
  "grade_level": 1,
  "academic_year": "2024-2025"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| school_id | string (UUID) | Yes | ID of the school | Must be a valid UUID |
| name | string | Yes | Name of the class | Max 100 characters |
| grade_level | integer | Yes | Grade level of the class | Between 1 and 12 |
| academic_year | string | Yes | Academic year | Non-empty string |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "school_id": "uuid",
  "name": "Class 1A Updated",
  "grade_level": 1,
  "academic_year": "2024-2025",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-02T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data or class ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Class not found
- `500 Internal Server Error` - Server errors

### Delete Class

Deletes a class.

**Endpoint:** `DELETE /api/classes/{id}`

**Authentication:** Required (Admin only)

**Authorization:** Admin only

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the class |

**Success Response (204 No Content)**

**Error Responses:**
- `400 Bad Request` - Invalid class ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Class not found
- `500 Internal Server Error` - Server errors

## Subjects API

### Create Subject

Creates a new subject.

**Endpoint:** `POST /api/subjects`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teachers can only create subjects in their school
- Admins can create subjects in any school

**Request Body:**
```json
{
  "school_id": "uuid",
  "name": "Mathematics",
  "code": "MATH101",
  "description": "Basic mathematics course"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| school_id | string (UUID) | Yes | ID of the school | Must be a valid UUID |
| name | string | Yes | Name of the subject | Max 100 characters |
| code | string | Yes | Code of the subject | Max 20 characters |
| description | string | No | Description of the subject | Max 500 characters |

**Success Response (201 Created):**
```json
{
  "id": "uuid",
  "school_id": "uuid",
  "name": "Mathematics",
  "code": "MATH101",
  "description": "Basic mathematics course",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `500 Internal Server Error` - Server errors

### Get All Subjects

Retrieves all subjects with optional filtering.

**Endpoint:** `GET /api/subjects`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teachers can only see subjects in their school
- Admins can see all subjects

**Query Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| school_id | string (UUID) | No | Filter by school |

**Success Response (200 OK):**
```json
[
  {
    "id": "uuid",
    "school_id": "uuid",
    "name": "Mathematics",
    "code": "MATH101",
    "description": "Basic mathematics course",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
]
```

**Error Responses:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `500 Internal Server Error` - Server errors

### Get Subject by ID

Retrieves a specific subject by ID.

**Endpoint:** `GET /api/subjects/{id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- User must belong to the same school as the subject or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the subject |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "school_id": "uuid",
  "name": "Mathematics",
  "code": "MATH101",
  "description": "Basic mathematics course",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid subject ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Subject not found
- `500 Internal Server Error` - Server errors

### Update Subject

Updates an existing subject.

**Endpoint:** `PUT /api/subjects/{id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must belong to the same school as the subject
- Admins can update any subject

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the subject |

**Request Body:**
```json
{
  "school_id": "uuid",
  "name": "Advanced Mathematics",
  "code": "MATH201",
  "description": "Advanced mathematics course"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| school_id | string (UUID) | Yes | ID of the school | Must be a valid UUID |
| name | string | Yes | Name of the subject | Max 100 characters |
| code | string | Yes | Code of the subject | Max 20 characters |
| description | string | No | Description of the subject | Max 500 characters |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "school_id": "uuid",
  "name": "Advanced Mathematics",
  "code": "MATH201",
  "description": "Advanced mathematics course",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-02T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data or subject ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Subject not found
- `500 Internal Server Error` - Server errors

### Delete Subject

Deletes a subject.

**Endpoint:** `DELETE /api/subjects/{id}`

**Authentication:** Required (Admin only)

**Authorization:** Admin only

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the subject |

**Success Response (204 No Content)**

**Error Responses:**
- `400 Bad Request` - Invalid subject ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Subject not found
- `500 Internal Server Error` - Server errors

## Class Roster Management API

### Add Student to Class

Adds a student to a class.

**Endpoint:** `POST /api/classes/{class_id}/students`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must be assigned to the class or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| class_id | string (UUID) | Yes | ID of the class |

**Request Body:**
```json
{
  "student_id": "uuid"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| student_id | string (UUID) | Yes | ID of the student | Must be a valid UUID |

**Success Response (201 Created):**
```json
{
  "id": "uuid",
  "class_id": "uuid",
  "student_id": "uuid",
  "status": "active",
  "created_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data or ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Class or student not found
- `409 Conflict` - Student is already enrolled in this class
- `500 Internal Server Error` - Server errors

### Get Class Roster

Retrieves all students enrolled in a class.

**Endpoint:** `GET /api/classes/{class_id}/students`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must be assigned to the class or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| class_id | string (UUID) | Yes | ID of the class |

**Success Response (200 OK):**
```json
[
  {
    "id": "uuid",
    "class_id": "uuid",
    "student_id": "uuid",
    "student_name": "John Doe",
    "student_email": "john.doe@example.com",
    "status": "active",
    "created_at": "2024-01-01T00:00:00Z"
  }
]
```

**Error Responses:**
- `400 Bad Request` - Invalid class ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Class not found
- `500 Internal Server Error` - Server errors

### Remove Student from Class

Removes a student from a class.

**Endpoint:** `DELETE /api/classes/{class_id}/students/{student_id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must be assigned to the class or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| class_id | string (UUID) | Yes | ID of the class |
| student_id | string (UUID) | Yes | ID of the student |

**Success Response (204 No Content)**

**Error Responses:**
- `400 Bad Request` - Invalid ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Student is not enrolled in this class
- `500 Internal Server Error` - Server errors

### Update Student Enrollment Status

Updates a student's enrollment status in a class.

**Endpoint:** `PUT /api/classes/{class_id}/students/{student_id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must be assigned to the class or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| class_id | string (UUID) | Yes | ID of the class |
| student_id | string (UUID) | Yes | ID of the student |

**Request Body:**
```json
{
  "status": "inactive"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| status | string | Yes | Enrollment status | Must be one of: "active", "inactive", "graduated" |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "class_id": "uuid",
  "student_id": "uuid",
  "status": "inactive",
  "created_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data or ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Student is not enrolled in this class
- `500 Internal Server Error` - Server errors

## Materials API

### Upload Teaching Material

Uploads a new teaching material.

**Endpoint:** `POST /api/materials`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teachers can only upload materials for classes they teach
- Admins can upload to any class

**Content Type:** multipart/form-data

**Form Fields:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| class_id | string (UUID) | Yes | UUID of the class | Must be a valid UUID |
| subject_id | string (UUID) | Yes | UUID of the subject | Must be a valid UUID |
| title | string | Yes | Title of the material | Max 255 characters |
| description | string | No | Description of the material | Max 1000 characters |
| visibility | string | No | Visibility level | Must be one of: "public", "private", "class_only" |
| file | file | Yes | The file to upload | Required file |

**Success Response (201 Created):**
```json
{
  "id": "uuid",
  "class_id": "uuid",
  "subject_id": "uuid",
  "teacher_id": "uuid",
  "title": "Algebra Basics",
  "description": "Introduction to algebra concepts",
  "file_name": "algebra_basics.pdf",
  "file_type": "application/pdf",
  "file_size": 1024000,
  "visibility": "class_only",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Class or subject not found
- `500 Internal Server Error` - Server errors

### Get All Materials

Retrieves all materials with optional filtering.

**Endpoint:** `GET /api/materials`

**Authentication:** Required (Any authenticated user)

**Authorization:** 
- Users can see public materials
- Students can see materials for their classes
- Teachers can see materials for classes they teach
- Admins can see all materials

**Query Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| class_id | string (UUID) | No | Filter by class |
| subject_id | string (UUID) | No | Filter by subject |
| teacher_id | string (UUID) | No | Filter by teacher |
| visibility | string | No | Filter by visibility |

**Success Response (200 OK):**
```json
[
  {
    "id": "uuid",
    "class_id": "uuid",
    "subject_id": "uuid",
    "teacher_id": "uuid",
    "title": "Algebra Basics",
    "description": "Introduction to algebra concepts",
    "file_name": "algebra_basics.pdf",
    "file_type": "application/pdf",
    "file_size": 1024000,
    "visibility": "class_only",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
]
```

**Error Responses:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `500 Internal Server Error` - Server errors

### Get Material by ID

Retrieves a specific material by ID.

**Endpoint:** `GET /api/materials/{id}`

**Authentication:** Required (Any authenticated user)

**Authorization:** 
- Users can access public materials
- Students can access materials for their classes
- Teachers can access materials for classes they teach
- Admins can access all materials

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the material |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "class_id": "uuid",
  "subject_id": "uuid",
  "teacher_id": "uuid",
  "title": "Algebra Basics",
  "description": "Introduction to algebra concepts",
  "file_name": "algebra_basics.pdf",
  "file_type": "application/pdf",
  "file_size": 1024000,
  "visibility": "class_only",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-01T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid material ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Material not found
- `500 Internal Server Error` - Server errors

### Update Material

Updates an existing material.

**Endpoint:** `PUT /api/materials/{id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must be the owner of the material or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the material |

**Request Body:**
```json
{
  "title": "Algebra Basics Updated",
  "description": "Updated introduction to algebra concepts",
  "visibility": "public"
}
```

**Request Parameters:**
| Field | Type | Required | Description | Validation |
|-------|------|----------|-------------|------------|
| title | string | Yes | Title of the material | Max 255 characters |
| description | string | No | Description of the material | Max 1000 characters |
| visibility | string | No | Visibility level | Must be one of: "public", "private", "class_only" |

**Success Response (200 OK):**
```json
{
  "id": "uuid",
  "class_id": "uuid",
  "subject_id": "uuid",
  "teacher_id": "uuid",
  "title": "Algebra Basics Updated",
  "description": "Updated introduction to algebra concepts",
  "file_name": "algebra_basics.pdf",
  "file_type": "application/pdf",
  "file_size": 1024000,
  "visibility": "public",
  "created_at": "2024-01-01T00:00:00Z",
  "updated_at": "2024-01-02T00:00:00Z"
}
```

**Error Responses:**
- `400 Bad Request` - Invalid request data or material ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Material not found
- `500 Internal Server Error` - Server errors

### Delete Material

Deletes a material.

**Endpoint:** `DELETE /api/materials/{id}`

**Authentication:** Required (Teacher or Admin)

**Authorization:** 
- Teacher must be the owner of the material or be an admin

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the material |

**Success Response (204 No Content)**

**Error Responses:**
- `400 Bad Request` - Invalid material ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Material not found
- `500 Internal Server Error` - Server errors

### Download Material

Downloads a material file.

**Endpoint:** `GET /api/materials/{id}/download`

**Authentication:** Required (Any authenticated user)

**Authorization:** 
- Users can download public materials
- Students can download materials for their classes
- Teachers can download materials for classes they teach
- Admins can download all materials

**Path Parameters:**
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| id | string (UUID) | Yes | ID of the material |

**Success Response:** The file content with appropriate Content-Type and Content-Disposition headers

**Error Responses:**
- `400 Bad Request` - Invalid material ID format
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Material or file not found
- `500 Internal Server Error` - Server errors

## Data Models

### Class Model

```json
{
  "id": "string (UUID)",
  "school_id": "string (UUID)",
  "name": "string",
  "grade_level": "integer",
  "academic_year": "string",
  "created_at": "string (ISO 8601 datetime)",
  "updated_at": "string (ISO 8601 datetime)"
}
```

### Subject Model

```json
{
  "id": "string (UUID)",
  "school_id": "string (UUID)",
  "name": "string",
  "code": "string",
  "description": "string",
  "created_at": "string (ISO 8601 datetime)",
  "updated_at": "string (ISO 8601 datetime)"
}
```

### Material Model

```json
{
  "id": "string (UUID)",
  "class_id": "string (UUID)",
  "subject_id": "string (UUID)",
  "teacher_id": "string (UUID)",
  "title": "string",
  "description": "string",
  "file_name": "string",
  "file_path": "string",
  "file_type": "string",
  "file_size": "integer",
  "visibility": "string (public, private, class_only)",
  "created_at": "string (ISO 8601 datetime)",
  "updated_at": "string (ISO 8601 datetime)"
}
```

### Student Enrollment Model

```json
{
  "id": "string (UUID)",
  "class_id": "string (UUID)",
  "student_id": "string (UUID)",
  "status": "string (active, inactive, graduated)",
  "created_at": "string (ISO 8601 datetime)"
}
```

## Error Responses

The API uses standard HTTP status codes to indicate the success or failure of requests. All error responses include a JSON object with error details.

**Error Response Format:**
```json
{
  "error": "string",
  "message": "string"
}
```

**Common Error Codes:**
- `400 Bad Request` - Invalid request data
- `401 Unauthorized` - Missing or invalid authentication
- `403 Forbidden` - Insufficient permissions
- `404 Not Found` - Resource not found
- `409 Conflict` - Resource conflict
- `422 Unprocessable Entity` - Validation errors
- `500 Internal Server Error` - Server errors

## Integration Information

The T1.7 Class & Content Management API integrates with the existing Rangkai Edu authentication and authorization systems. It uses the same JWT-based authentication mechanism and role-based access control as the rest of the application.

### Dependencies

1. **Authentication System**: All endpoints require a valid JWT token obtained through the existing authentication flow.
2. **Database**: The API interacts with the PostgreSQL database using the existing database connection patterns.
3. **File Storage**: The materials upload functionality uses the local filesystem for storing uploaded files.
4. **User Management**: The API relies on the existing user management system to verify user roles and permissions.

### Implementation Notes

1. **File Upload Security**: 
   - File types and extensions are validated
   - File sizes are limited
   - Files are stored outside the web root
   - File names are sanitized

2. **Access Control**:
   - Role-based access is enforced at the API level
   - Ownership validation is performed for modification operations
   - Proper error handling prevents information leakage

3. **Data Validation**:
   - All input parameters are validated
   - Parameterized queries prevent SQL injection
   - Rate limiting helps prevent abuse